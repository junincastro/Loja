@model LojaVirtual.Dominio.Entidades.Carrinho


<div class="navbar-text navbar-left">
    <b>Seus produtos:</b>
    <b>@Model.ItensCarrinho.Sum(x => x.Quantidade) item(s), @Model.ObterValorTotal().ToString("c")</b>
</div>

<div class="navbar-left">
    @Html.ActionLink("Exibir", "Index", "Carrinho", new { returnUrl = Request.Url.PathAndQuery }, new { @class = "btn btn-default navbar-btn" })
</div>